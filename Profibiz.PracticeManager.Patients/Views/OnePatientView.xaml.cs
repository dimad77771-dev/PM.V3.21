using System.Windows;
using System.Windows.Controls;

namespace Profibiz.PracticeManager.Patients.Views
{
    /// <summary>
    /// Interaction logic for PatientsView
    /// </summary>
    public partial class OnePatientView
	{
        public OnePatientView()
        {
            InitializeComponent();


			//var template = q999.HeaderTemplate;
			//var xaml = System.Windows.Markup.XamlWriter.Save(template);
			//var a = this.Height;

			//object defaultStyleKey = aaaaa.GetValue(FrameworkElement.DefaultStyleKeyProperty);
			// Извлечь ресурс с этим ключом
			//Style style = (Style)Application.Current.FindResource(defaultStyleKey);
			// Сериализовать его XAML-представлениев виде строки 
			//string xaml = System.Windows.Markup.XamlWriter.Save(style);

			//aaaaa.IsChecked = true;

			//var b1 = bbbb.GetValue(FrameworkElement.DefaultStyleKeyProperty);
			//var b2 = ddd.GetValue(FrameworkElement.DefaultStyleKeyProperty);
			//Style style = (Style)Application.Current.FindResource(b2);
			//string xaml = System.Windows.Markup.XamlWriter.Save(style);
		}
	}
}
